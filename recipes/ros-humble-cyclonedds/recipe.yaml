package:
  name: ros-humble-cyclonedds
  version: 0.9.1
source:
  git_url: https://github.com/ros2-gbp/cyclonedds-release.git
  git_rev: release/humble/cyclonedds/0.9.1-1
  folder: ros-humble-cyclonedds/src/work

build:
  script:
    sel(win): bld_catkin.bat
    sel(unix): build_catkin.sh
  number: 3
about:
  home: https://www.ros.org/
  license: BSD-3-Clause
  summary: |
    Robot Operating System

extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - "{{ compiler('cxx') }}"
    - "{{ compiler('c') }}"
    - ninja
    - setuptools
    - sel(unix): make
    - sel(unix): coreutils
    - sel(osx): tapi
    - sel(build_platform != target_platform): pkg-config
    - cmake
    - sel(build_platform != target_platform): python
    - sel(build_platform != target_platform): cross-python_{{ target_platform }}
    - sel(build_platform != target_platform): cython
    - sel(build_platform != target_platform): numpy
    - sel(build_platform != target_platform): ros-humble-cyclonedds
  host:
    - cunit
    - numpy
    - openssl
    - pip
    - sel(build_platform == target_platform): pkg-config
    - python
    - ros-humble-iceoryx-binding-c
    - ros-humble-iceoryx-hoofs
    - ros-humble-iceoryx-posh
    - ros-humble-ros-environment
    - ros-humble-ros-workspace
    - ros2-distro-mutex 0.3 humble
  run:
    - openssl
    - python
    - ros-humble-iceoryx-binding-c
    - ros-humble-iceoryx-hoofs
    - ros-humble-iceoryx-posh
    - ros-humble-ros-workspace
    - ros2-distro-mutex 0.3 humble
    - sel(osx and x86_64): __osx >={{ MACOSX_DEPLOYMENT_TARGET|default('10.14') }}
